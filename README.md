# DVC + Parquet + PyTorch Template

## Overview

This project shows how to manage large-scale text classification datasets using DVC and Parquet, and integrate with PyTorch for training.

## Usage

```bash
dvc repro          # runs preprocessing and training
dvc push           # uploads data to remote
dvc pull           # downloads data from remote
```
